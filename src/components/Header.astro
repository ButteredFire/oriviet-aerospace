---
const logoPath = '/Logos/OrivietAerospaceLogo-Mono.svg';
---

<header class="header bg-black px-4 py-3 shadow-md h-16">
    <nav class="container mx-auto flex items-center justify-center">
        <a href="/" class="absolute left-4 top-1/2 -translate-y-1/2 flex items-center h-full"> {/* Make the 'a' tag take full height of nav */}
            <img
              src={logoPath}
              alt="Oriviet Aerospace Logo"
              class="h-10 w-auto object-contain"> {/* Set a specific height for the image, let width auto-scale */}
        </a>

        <ul class="flex-grow flex justify-center items-center space-x-6">
            <li><a href="/research" class="font-bold nav-link uppercase">Research & Development</a></li>
            <li><a href="/team" class="font-bold nav-link uppercase">Team</a></li>
            
            
            <li>
                <div class="nav-link-disabled font-bold uppercase relative group">
                    About
                    <span class="tooltip absolute top-full left-1/2 -translate-x-1/2 mt-2 px-2 py-1 bg-gray-700 text-white text-xs rounded opacity-0 group-hover:opacity-100 transition-opacity duration-200 pointer-events-none whitespace-nowrap">Coming Soon</span>
                </div>
            </li>
            <li>
                <div class="nav-link-disabled font-bold uppercase relative group">
                    Roadmap
                    <span class="tooltip absolute top-full left-1/2 -translate-x-1/2 mt-2 px-2 py-1 bg-gray-700 text-white text-xs rounded opacity-0 group-hover:opacity-100 transition-opacity duration-200 pointer-events-none whitespace-nowrap">Coming Soon</span>
                </div>
            </li>
        </ul>
    </nav>
</header>


<style>
    .header {
        position: sticky;
        top: 0;
        left: 0;
        display: flex;
        flex-shrink: 0;
        z-index: 999;
    }

    .nav-link {
        position: relative;
        padding-bottom: 2px; /* Space for the underline */
        transition: color 0.2s ease-in-out;
    }
    .nav-link::after {
        content: '';
        position: absolute;
        bottom: 0;
        right: 0;
        width: 0%;
        height: 1px; /* Underline thickness */
        background-color: white; /* Underline color */
        transition: width 0.2s ease-in-out, background-color 0.2s ease-in-out; /* Animate width and color */
    }
    .nav-link:hover::after {
        width: 100%; /* Expand underline on hover */
        left: 0;
        right: auto;
    }

    .nav-link-disabled {
        color: #888; /* Dim the text to indicate it's disabled */
        cursor: not-allowed; /* Change cursor to "not-allowed" */
        pointer-events: auto; /* Allow hover events for the tooltip */
    }

    .nav-link-disabled .tooltip {
        /* Tooltip specific styles are inlined with Tailwind for simplicity */
    }
</style>